# HatchSeal 种禽场种蛋批入孵前联合检验闭环

## 项目目标

面向种禽场孵化中心的后端服务，以种蛋批号和蛋盘封签为主线，将来源核验、双人收蛋、资源租约、照蛋覆盖、拭子培养、沙门氏菌快检、理化复测、污染复判、独立复核和终局裁定收束为一个可恢复流程。实现采用 Go HTTP API、关系型持久化与事务性事件/版本记录；所有并发占用和终局写入由数据库约束、任务代次及比较更新共同裁定。标准规模控制为20至28个生产Go文件、约2500有效生产行且总量保持在2000至3000行，分布于至少4个业务包；公开测试不少于4个测试文件和12个确定性用例。

## 端到端业务流程

1. 建档锁定：操作员提交父母代鸡舍、采蛋班次、熏蒸柜批次及摘要、种蛋批号、蛋盘封签与格位、入孵器时隙、照蛋窗口、盲码、培养板孔、快检板孔、阈值快照、人员名单和任务代次；系统在单事务内验证目录匹配并建立一次性锁定与资源租约。
2. 收蛋与启动：两名不同且具备收蛋资格的人员按当前代次确认实物和封签；相同操作号同内容可幂等重放，内容不同则拒绝。双人确认完成后，任务原子进入资源占用中并启动检测，任何失败不留下部分租约。
3. 联合采集：按锁定蛋盘格位提交照蛋分类和补测标记，形成不可非法覆盖的活力矩阵；同时记录蛋壳拭子培养版本、沙门氏菌快检版本以及蛋重、气室高度、洁净度和熏蒸残留的定点整数证据。外部设备调用失败只生成可审计的待重试记录。
4. 揭盲与污染复判：仅在规定阶段揭示盲码，核对三联样本；活力不足、裂纹越界、疑阳、培养污染或样本分歧只能为当前代次创建一个复判案，明确受影响蛋盘、格位、盲码和检测孔位，并以新版本追加复判证据。
5. 独立复核：全部覆盖与检测证据闭合后，两名不同的合格复核人员分别签署结论；复核人员不得与收蛋人员重合，也不得互相重复，复核操作必须匹配当前任务代次。
6. 终局竞争：满足阈值和双重复核时可申请允许入孵并生成唯一入孵凭据；污染成立可申请污染隔离，授权人员也可申请取消。三种命令竞争同一单写屏障，只有首个提交成功，终态后的任何迟到读数或普通操作均稳定拒绝。

## 核心组件与职责

1. 种禽舍与入孵规则目录：管理鸡舍、采蛋班次、熏蒸柜批次摘要、设备、人员资格、固定小数精度和检验阈值的可引用版本；锁定后任务只读取快照。
2. 种蛋入孵任务聚合：维护十二种业务状态、任务代次、双人收蛋、操作号幂等记录、状态转换和终态保护，并协调原子事务。
3. 蛋盘样本与资源占用账簿：保存蛋盘封签、格位、盲码及入孵器时隙、照蛋窗口、培养板孔和快检板孔租约，通过唯一约束、租约代次和逻辑到期时间处理并发占用。
4. 照蛋活力及理化采集账簿：维护格位覆盖矩阵、分类计数守恒、补测记录、定点整数理化证据、外部设备调用及确定性重试计划。
5. 污染复判及终局仲裁器：保存不可覆盖的培养、快检与复判版本链，计算证据闭合和阈值结果，执行双人独立复核、唯一凭据生成及入孵/隔离/取消单写竞争。
6. Go HTTP API：提供JSON命令、查询和审计接口，执行输入限制、稳定错误码、确定性原因排序、事务边界、请求追踪及健康检查。

## 领域规则与不变量

1. 任务状态严格为待锁定、待收蛋确认、资源占用中、照蛋采集中、拭子培养中、快检核验中、理化复测中、待独立复核、可入孵、已入孵、污染隔离、已取消；转换由已闭合证据驱动，不允许跳过必经阶段。
2. 种蛋批号与蛋盘封签一经锁定不得换绑；同一开放任务范围内，种蛋批号、封签、盲码、入孵器时隙、照蛋窗口、培养板孔和快检板孔分别全局唯一占用。换窗必须在单事务中先验证目标、再交换租约，失败保留原租约。
3. 熏蒸摘要必须与目录当前版本一致，鸡舍和采蛋班次必须存在有效关联；陈旧摘要或来源错配不得推进任务。盲码在揭盲前不得经任何查询或错误详情泄露，重复盲码及提前揭盲均被拒绝。
4. 每个锁定格位只能有一个当前有效覆盖结果；受精、未受精、裂纹、血斑和污染斑采用预定义互斥或附加标记规则，主分类合计必须等于提交格位数，重复或缺失格位不得形成有效覆盖，补测只能追加新版本。
5. 蛋重、气室高度、洁净度、菌落数、Ct值和熏蒸残留以字段规定的小数位解析成有符号64位整数；检查文本长度、符号、精度、除零、乘加溢出及业务上下界。边界比较统一使用锁定阈值和明确的包含规则，算术失败不落派生证据。
6. 设备拒绝、断连、超时或格式错误只追加调用尝试，状态为待重试；重试键由任务、设备、证据种类、对象和代次确定，次数递增且下一逻辑时间可计算，设备失败不得生成合格值或释放租约。
7. 培养、快检和复判记录均为追加式版本链。每个任务代次至多存在一个活动复判案；代次提升后旧读数仍可审计但不得成为当前证据，冲突代次提交返回稳定错误。
8. 收蛋人员必须两人不同；独立复核人员必须两人不同、具备对应资格且不属于该任务收蛋人员。操作号在任务内唯一，相同规范化内容返回原结果，不同内容返回操作号冲突。所有拒绝原因按鸡舍、种蛋批号、蛋盘封签、格位、检测孔位及错误码依次稳定排序。

## 数据模型与持久化

1. CatalogHouse、CollectionShift、FumigationBatch、IncubatorSlot、CandlingWindow、Device、PersonQualification和RuleSetVersion：保存目录关系、有效期、摘要版本、人员角色及精度/阈值定义。
2. IncubationTask：包含任务ID、种蛋批号、状态、代次、规则快照、熏蒸摘要快照、版本号、逻辑时间、创建时间和终局字段；以版本号支持乐观并发更新。
3. TraySeal、TrayPosition和BlindSample：描述封签及确定排序的格位集合、盲码密文/摘要、揭盲状态和揭盲版本；开放任务唯一索引约束批号、封签和盲码。
4. ResourceLease：记录资源类型、规范资源键、任务、代次、获得和逻辑到期时间、释放原因；开放租约唯一索引覆盖入孵器时隙、照蛋窗口及两类板孔。
5. CandlingEntry与PhysicochemicalEvidence：按任务、封签、格位、证据类型和版本保存照蛋分类、附加缺陷、补测、蛋重、气室、洁净度和熏蒸残留的原始整数及派生判断。
6. CultureEvidence、RapidTestEvidence与RetestCase：分别保存培养孔菌落、快检孔Ct值、设备原文摘要、证据版本、当前性标记，以及复判代次、受影响封签/格位/盲码/孔位和裁定。
7. ReceiptConfirmation、IndependentReview、OperationDedup与DeviceAttempt：保存人员、角色、代次、规范内容哈希、原响应、设备故障类别、尝试次数和下一逻辑重试时间。
8. FinalDecision与IncubationCredential：以任务唯一键实现单写终局，记录允许入孵、污染隔离或取消、竞争版本、证据摘要；仅允许入孵生成一个不可变凭据编号。数据库迁移和事务日志支持进程重启后恢复开放任务、租约、重试与版本链。

## 公开接口

1. POST /v1/tasks 与 POST /v1/tasks/{id}/lock：建立草稿并原子锁定来源、规则、封签、格位、样本及全部初始资源；命令携带operation_id和generation。
2. POST /v1/tasks/{id}/receipts、/start、/window-exchanges：完成双人收蛋、原子启动或换窗，返回当前状态、任务版本和逐项租约结果。
3. POST /v1/tasks/{id}/candling、/swabs/seal、/cultures/readings、/rapid-tests/readings、/physicochemical：批量提交格位或孔位证据，整批验证失败则整批回滚。
4. POST /v1/tasks/{id}/blind/reveal、/retests、/retests/{generation}/evidence：受控揭盲、创建唯一复判案并追加指定代次证据。
5. POST /v1/tasks/{id}/reviews 与 POST /v1/tasks/{id}/decisions/{admit|isolate|cancel}：执行独立复核和终局竞争；成功入孵后可通过GET /v1/tasks/{id}/credential读取凭据。
6. GET /v1/tasks/{id}、/evidence、/leases、/audit与GET /healthz：以稳定字段顺序展示聚合状态、覆盖缺口、版本证据、资源占用、设备重试和审计记录；错误统一为code、message、reasons及operation_id。

## 失败边界

1. 所有命令以任务事务为边界：目录校验、幂等登记、租约变化、覆盖写入、证据追加、状态推进和审计记录必须共同提交；任何校验、算术、唯一约束或并发版本失败均整体回滚。
2. 利用数据库唯一索引和带版本条件的UPDATE处理锁定、换窗、启动及终局竞争；约束冲突映射为稳定业务错误，不把数据库文本暴露给调用方。
3. 批量输入先规范化并完整验证，再执行写入；错误原因收集后按领域键排序，因此映射遍历、请求顺序和并发调度不会改变响应。
4. 外部设备通过端口接口隔离，调用结果先记录为DeviceAttempt，再由明确成功响应生成业务证据；崩溃重启后依据持久化尝试状态和逻辑时钟继续，不重复伪造调用结论。
5. 迟到输入同时校验终态、任务代次、资源租约代次和证据链头；任何一项陈旧均只返回错误，不改变揭盲状态、覆盖矩阵、复判版本或最终结论。
6. 进程重启后从数据库恢复开放任务和未完成重试；租约只按持久化逻辑时间和明确命令到期，墙钟漂移或服务重启不会提前释放资源。

## 验收标准

1. 锁定命令完整冻结鸡舍、班次、熏蒸批次及摘要、批号、封签、格位、入孵器时隙、照蛋窗口、盲码、两类板孔、全部阈值和任务代次；任一错配使事务无写入。
2. 并发任务争夺任一批号、封签、盲码、时隙、窗口或孔位时恰有一个获得有效占用；并发锁定、启动和换窗均原子完成，失败者不持有部分资源。
3. 收蛋确认、照蛋、拭子封存、揭盲和复核均校验当前代次与操作号；同号同内容返回原结果，同号异内容、陈旧熏蒸摘要或错误鸡舍返回稳定错误。
4. 照蛋有效矩阵恰好覆盖全部锁定格位，分类与缺陷计数满足整数守恒；缺格、重格和非法计数整批回滚。
5. 所有理化、培养和快检值使用定点整数安全解析与边界比较；长度、符号、除零、溢出、菌落越界或格式失败均不产生派生证据。
6. 可控设备故障只形成持久化待重试尝试，重试对象、次数和逻辑时间完全确定；重启后可以继续，且资源不被提前释放。
7. 污染触发条件只产生一个当前代次复判案并覆盖全部受影响对象；旧代次迟到证据保留但不覆盖版本链头，也不改变当前结论。
8. 证据全部闭合且两名合格、互异并与收蛋人员分离的复核者通过后，允许入孵、污染隔离和取消的并发竞争只能提交一个终局；入孵凭据唯一，终态后所有写操作被拒绝。

## 确定性测试场景

1. test/task_lock_test.go：完整锁定快照；鸡舍/班次错配；陈旧熏蒸摘要；重复封签或盲码回滚。
2. test/resource_race_test.go：同步屏障驱动两个任务竞争入孵器时隙、窗口和板孔；验证唯一胜者及失败者零部分租约；并发换窗保持原占用。
3. test/idempotency_generation_test.go：同操作号同内容重试；同号异内容冲突；陈旧任务代次提交；提前揭盲且响应不泄露盲码。
4. test/candling_matrix_test.go：完整格位覆盖；缺失与重复格位；受精/未受精主分类不守恒；裂纹、血斑、污染斑和补测边界。
5. test/fixedpoint_threshold_test.go：蛋重、气室、洁净度、Ct值和残留阈值等于边界；超长、负号、精度错误、除零和int64乘加溢出；菌落计数越界。
6. test/device_retry_test.go：脚本化照蛋仪拒绝、培养箱断连、读板器超时和电子秤格式错误；断言尝试次数、下一逻辑时间、重启恢复及无伪造证据。
7. test/retest_version_test.go：疑阳、培养污染和三联样本分歧共同触发单个复判案；并发创建复判；旧代次迟到读数不影响当前链头。
8. test/review_final_race_test.go：收蛋与复核角色重合；复核人重复；证据未闭合；入孵、隔离和取消三方同步竞争；唯一凭据及终态迟到操作拒绝。

## 组件追踪关系

1. 种禽舍与入孵规则目录对应锁定来源、摘要和阈值快照，主要由目录模型、规则校验、快照服务及迁移文件实现，预计4至5个生产Go文件。
2. 种蛋入孵任务聚合对应十二状态、代次、收蛋和幂等语义，主要由聚合根、状态机、命令服务、幂等仓储及事务协调实现，预计5至6个生产Go文件。
3. 蛋盘样本与资源占用账簿对应格位、盲码和四类并发租约，主要由样本模型、租约模型、占用服务及SQL仓储实现，预计4至5个生产Go文件。
4. 照蛋活力及理化采集账簿对应覆盖守恒、定点算术和设备重试，主要由矩阵校验、定点数、证据服务、设备端口和重试调度实现，预计5至6个生产Go文件。
5. 污染复判及终局仲裁器对应版本链、复判、独立复核和单写终局，主要由复判服务、闭合评估器、复核策略、终局仓储和凭据生成实现，预计4至5个生产Go文件。
6. Go HTTP API对应命令/查询路由、DTO校验、稳定错误映射和启动恢复，预计4至5个生产Go文件；整体保持6个核心组件、至少4个Go包、20至28个生产文件和约2500有效行。

## 独特性

项目聚焦种禽场单次种蛋批入孵前的联合检验，以蛋盘格位级活力覆盖、双板孔检测、盲码三联样本、复判代次隔离和入孵终局单写屏障形成独特闭环；它不同于既有农产品入厂、放播、转场或加工放行项目，也不扩展到养殖排产、饲料、销售或孵化后的雏鸡管理。
